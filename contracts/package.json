{
  "name": "@qevie/contracts",
  "private": true,
  "scripts": {
    "build": "PATH=$HOME/.foundry/bin:$PATH forge build --offline",
    "typecheck": "PATH=$HOME/.foundry/bin:$PATH forge build --offline",
    "lint": "PATH=$HOME/.foundry/bin:$PATH forge fmt --check",
    "format": "PATH=$HOME/.foundry/bin:$PATH forge fmt",
    "test": "PATH=$HOME/.foundry/bin:$PATH forge test --offline --force"
  }
}
