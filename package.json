{
  "name": "qevie",
  "private": true,
  "packageManager": "pnpm@10.33.4",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "contracts:build": "forge build --offline --root contracts",
    "contracts:test": "forge test --offline --force --root contracts"
  }
}
